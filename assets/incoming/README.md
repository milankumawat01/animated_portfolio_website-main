# Drop raw assets here

See ../../docs/04-ASSET-MANIFEST.md for exact specs (formats, sizes, filenames).

The P5 pipeline reads this folder and writes optimized output into public/.
Do not put raw files in public/ yourself.
